rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Safe baseline. Feature-specific Rules tests replace this deny-all policy deliberately.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
